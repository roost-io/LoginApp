# ********RoostGPT********

# Test generated by RoostGPT for test Loginapp-api-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /viewport_get for http method type GET in artillery framework
# RoostTestHash=851b3012b5
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins: 
    expect: {}
  payload:
    path: "viewport_get_others.csv"
    skipHeader: true
    fields:
      - "InvalidField1"
      - "InvalidField2"
    delimiter: "^|^"
scenarios:
  - name: "Test with various non-successful API requests"
    flow:
      - log: "Testing - GET request with incorrect URL"
      - get:
          url: "/incorrecturl"
          expect:
            - hasHeader: "content-type"
            - statusCode: 404
      - think: 1
      - log: "Testing - GET request with missing authentication"
      - get:
          url: "/viewport"
          headers:
            Content-Type: "application/json"
          expect:
            - hasHeader: "content-type"
            - statusCode: 401
      - think: 1
      - log: "Testing - GET request with invalid payload"
      - get:
          url: "/viewport"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          qs:
            InvalidField1: "{{ InvalidField1 }}"
            InvalidField2: "{{ InvalidField2 }}"
          expect:
            - hasHeader: "content-type"
            - statusCode: 400
      - think: 1
