# ********RoostGPT********

# Test generated by RoostGPT for test Loginapp-api-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /browser-compatibility_get for http method type GET in artillery framework
# RoostTestHash=1e2857e484
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "browser-compatibility_get_success.csv"
    skipHeader: true
    fields: []
    delimiter: "^|^"

scenarios:
  - name: "Checking login page compatibility on different browsers"
    flow:
      - log: "Starting the scenario for checking the browser compatibility"
      - post:
          url: "/browser-compatibility"
          headers:
            Authorization: "Token {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "description"
            - equals: 
                - description: "Successful operation - page is compatible"
      - think: 5
      - log: "Ending the scenario for checking the browser compatibility"
