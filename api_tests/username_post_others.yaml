# ********RoostGPT********

# Test generated by RoostGPT for test Loginapp-api-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /username_post for http method type POST in artillery framework
# RoostTestHash=ec45e1f0d1
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  payload:
    path: "username_post_others.csv"
    fields:
      - "username"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: Artillery test scenario for /username POST request
    flow:
      - log: "Sending a POST request to  /username with invalid payload"
      - post:
          url: "/username_invalid"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            username: "{{ username }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1
      
      - log: "Sending a POST request to /username without authorization token"
      - post:
          url: "/username"
          headers:
            Content-Type: "application/json"
          json:
            username: "{{ username }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
      - think: 1
      
      - log: "Sending a POST request to /username with valid payload and valid authorization token"
      - post:
          url: "/username"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            username: "{{ username }}"
          expect:
            - statusCode: 201
            - hasProperty: "id"
      - think: 1
