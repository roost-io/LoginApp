# ********RoostGPT********

# Test generated by RoostGPT for test Loginapp-api-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /login-button-click_get for http method type GET in artillery framework
# RoostTestHash=abe3f35eab
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "login-button-click_get_success.csv"
    fields: ["field1", "field2"]
    delimiter: "^|^"
    skipHeader: true
  plugins:
    expect: {} 

scenarios:
  - name: "Validate API Correct Behavior"
    flow: 
      - log: "Starting test for API"
      - think: 
          duration: 1
      - get:
          url: "/login-button-click?paramName={{ field1 }}"
          headers:
            AUTH_TOKEN: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect: 
            - statusCode: 200
            - hasProperty: "property1"
            - hasProperty: "property2"
            - equals: ["expectedKey", "expectedValue"]
          capture:
            json: "$.data"
            as: "data"
      - log: "data captured: {{ data }}"
      - think: 
          duration: 1
      - post:
          url: "/login-button-click"
          json:
            field1: "{{ field1 }}"
            field2: "{{ field2 }}"
          headers:
            AUTH_TOKEN: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect: 
            - statusCode: 200
            - hasProperty: "property1"
            - hasProperty: "property2"
            - equals: ["expectedKey", "expectedValue"]
          capture:
            json: "$.data"
            as: "data"
      - log: "data captured: {{ data }}"
      - think: 
          duration: 1
