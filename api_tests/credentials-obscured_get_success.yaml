# ********RoostGPT********

# Test generated by RoostGPT for test Loginapp-api-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /credentials-obscured_get for http method type GET in artillery framework
# RoostTestHash=86389d9c11
# 
# 

# ********RoostGPT********
config:
  target: '{{ $processEnvironment.API_HOST }}'
  plugins:
    expect: {}
  environments:
    production:
      target: '{{ $processEnvironment.API_HOST }}'
      plugins:
        expect: {}
    environmentVariable: 
      AUTH_TOKEN: '{{$processEnvironment.AUTH_TOKEN}}'
  payload:
    path: 'credentials-obscured_get_success.csv'
    fields:
      - 'username'
      - 'password'
    skipHeader: true
    delimiter: '^|^'

scenarios:
- name: 'Check username and password fields obscured status'
  flow:
    - log: 'Starting GET /credentials-obscured scenario'
    - get:
        url: '/credentials-obscured'
        headers:
          Authorization: 'Bearer {{$processEnvironment.AUTH_TOKEN}}'
        capture:
          - json: '$.obscured'
            as: 'obscured'
        expect:
          - statusCode: 200
          - hasProperty: 'obscured'
    - think: 1
