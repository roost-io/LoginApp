# ********RoostGPT********

# Test generated by RoostGPT for test Loginapp-api-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /login-button-status_get for http method type GET in artillery framework
# RoostTestHash=929dec4c12
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "login-button-status_get_others.csv"
    fields:
      - "email"
      - "password"
    skipHeader: true
    delimiter: "^|^"
scenarios:
  - name: "Testing non-success API responses"
    flow:
      - log: "Sending an invalid payload for login button status"
      - post:
          url: "/login-button-status/invalid"
          json:
            email: "{{ email }}"
            password: "{{ password }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - equals: ["message", "Invalid payload received"]
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          rejectUnauthorized: false
          timeout: 120000
      - think: 5
      - log: "Sending a request without authorization"
      - post:
          url: "/login-button-status"
          json:
            email: "{{ email }}"
            password: "{{ password }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - equals: ["message", "Missing Authorization Header"]
            - hasHeader: "WWW-Authenticate"
          rejectUnauthorized: false
          timeout: 120000
      - think: 5
      - log: "Sending a request to a non-existent endpoint"
      - post:
          url: "/invalid-endpoint"
          json:
            email: "{{ email }}"
            password: "{{ password }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - equals: ["message", "Endpoint Not Found"]
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          rejectUnauthorized: false
          timeout: 120000
      - think: 5
