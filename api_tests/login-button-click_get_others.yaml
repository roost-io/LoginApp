# ********RoostGPT********

# Test generated by RoostGPT for test Loginapp-api-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /login-button-click_get for http method type GET in artillery framework
# RoostTestHash=abe3f35eab
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "login-button-click_get_others.csv"
    fields: ["username", "password"]
    skipHeader: true
    delimiter: "^|^"
  http:
    timeout: 10
  phases:
    - duration: 60
      arrivalRate: 20
  defaults:
    headers:
      Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - flow:
      - post:
          url: "/login-button-click"
          json:
            username: "{{ username }}"
            password: "{{ incorrect_password }}"
          expect:
            - statusCode: 400
            - contentType: 'application/problem+json'
            - hasProperty: 'errors'
            - jmespath: errors[*].location INCLUDES(['body'])
        think: 3

      - post:
          url: "/login-button-click"
          headers:
            Authorization: ""
          json:
            username: "{{ username }}"
            password: "{{ password }}"
          expect:
            - statusCode: 401
            - contentType: 'application/problem+json'
            - hasProperty: 'detail'
            - matchesRegexp: 'detail RE_LOOSELY_MATCH([unauthorized])'
        think: 3

      - post:
          url: "/wrong-button-click"
          json:
            username: "{{ username }}"
            password: "{{ password }}"
          expect:
            - statusCode: 404
        think: 3
