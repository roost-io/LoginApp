# ********RoostGPT********

# Test generated by RoostGPT for test Loginapp-api-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /credentials-obscured_get for http method type GET in artillery framework
# RoostTestHash=86389d9c11
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 300
      arrivalRate: 5
  payload:
    path: "credentials-obscured_get_others.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}
scenarios:
  - name: Non-Success API Requests Scenario
    flow:
      - log: "Sending GET request to /credentials-obscured with incorrect URL"
      - get:
          url: "/credential-obscure"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
      - expect:
          - statusCode: 404
      - think: 1

      - log: "Sending GET request to /credentials-obscured without Authorization header"
      - get:
          url: "/credentials-obscured"
      - expect:
          - statusCode: 401
      - think: 1

      - log: "Sending GET request to /credentials-obscured with incorrect Authorization token"
      - get:
          url: "/credentials-obscured"
          headers:
            Authorization: "Bearer WrongToken"
      - expect:
          - statusCode: 401
      - think: 1

      - log: "Sending GET request to /credentials-obscured with invalid payload (empty username and password)"
      - get:
          url: "/credentials-obscured?username=&password="
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
      - expect:
          - statusCode: 400
      - think: 1

      - log: "Sending GET request to /credentials-obscured with valid settings (from payload)"
      - get:
          url: "/credentials-obscured?username={{ username }}&password={{ password }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
      - expect:
          - statusCode: 200
          - hasProperty: "username"
          - hasProperty: "password"
      - think: 1
