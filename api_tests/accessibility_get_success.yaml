# ********RoostGPT********

# Test generated by RoostGPT for test Loginapp-api-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /accessibility_get for http method type GET in artillery framework
# RoostTestHash=a6e0ff6cd5
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  environments:
    staging:
      target: "{{ $processEnvironment.API_HOST }}"
  payload:
    path: "accessibility_get_success.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Standard User Flow"
    flow:
      - log: "Calling Accessibility API Endpoint"
      - get:
          url: "/accessibility?username={{ username }}&password={{ password }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "pageAccessible"
            - equals:
                pageAccessible: true
      - think: 5
