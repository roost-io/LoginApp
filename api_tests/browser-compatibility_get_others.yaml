# ********RoostGPT********

# Test generated by RoostGPT for test Loginapp-api-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /browser-compatibility_get for http method type GET in artillery framework
# RoostTestHash=1e2857e484
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
  - duration: 5
    arrivalRate: 1
  plugins:
    expect: {}
  payload:
    path: "browser-compatibility_get_others.csv"
    fields:
      - "browser"
      - "version"
      - "os"
    skipHeader: true
    delimiter: "^|^"
scenarios:
- name: "API Negative Testing"
  flow:
  - log: "Requesting with incorrect authorization token"
  - get:
      url: "/browser-compatibility"
      headers:
        Authorization: "Bearer wrong_token"
      capture:
      - json: "$.message"
        as: "auth_fail_message"
      expect:
      - statusCode: 401
  - think: 1
  - log: "Requesting with missing headers"
  - get:
      url: "/browser-compatibility"
      expect:
      - statusCode: 400
  - think: 1
  - log: "Requesting with wrong type of header"
  - get:
      url: "/browser-compatibility"
      headers:
        Content-Type: "text/plain"
      expect:
      - statusCode: 415
  - think: 1
  - log: "Requesting with wrong URL"
  - get:
      url: "/browser-compatibility-wrong"
      headers:
        Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
      expect:
      - statusCode: 404
  - think: 1
  - log: "Requesting with wrong method"
  - post:
      url: "/browser-compatibility"
      headers:
        Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
      expect: 
      - statusCode: 405
  - think: 1
  - log: "Requesting with invalid payload data"
  - post:
      url: "/browser-compatibility"
      headers:
        Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
        Content-Type: "application/json"
      json: 
        browser: "invalid_browser"
        version: "invalid_version"
        os: "invalid_os"
      expect:
      - statusCode: 400
