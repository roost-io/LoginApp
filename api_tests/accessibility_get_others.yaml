# ********RoostGPT********

# Test generated by RoostGPT for test Loginapp-api-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /accessibility_get for http method type GET in artillery framework
# RoostTestHash=a6e0ff6cd5
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
    - duration: 600
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "accessibility_get_others.csv"
    fields:
      - "field1"
      - "field2"
    options:
      delimiter: "^|^"
      skipHeader: true

scenarios:
  - name: "API Failure/Edge case Testing: accessibility endpoints"
    flow:
      - log: "Testing accessibility: incorrect URL"
      - get:
          url: "/wrongEndpoint"
          expect:
            - statusCode: 404
            - hasProperty: "error"
      - think: 1
      
      - log: "Testing accessibility: unauthenticated access"
      - get:
          url: "/accessibility"
          expect:
            - statusCode: 401
      - think: 1
      - log: "Testing accessibility: authenticated, unacceptable payload"
      - post:
            url: "/accessibility"
            json:
              field1: "{{ field1 }}"
              field2: "invalid payload"
            headers:
              SECURITY_SCHEMA: "{{$processEnvironment.AUTH_TOKEN}}"
            expect: 
              - statusCode: 400
              - hasProperty: "error"
      - think: 1
      - log: "Testing accessibility: authenticated, empty payload"
      - post:
            url: "/accessibility"
            json: {}
            headers:
              SECURITY_SCHEMA: "{{$processEnvironment.AUTH_TOKEN}}"
            expect: 
                - statusCode: 400
                - hasProperty: "error"
