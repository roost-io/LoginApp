# ********RoostGPT********

# Test generated by RoostGPT for test Loginapp-api-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /login-page-load_get for http method type GET in artillery framework
# RoostTestHash=1d8a2f85a3
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "login-page-load_get_others.csv"
    fields: ["username", "password"]
    skipHeader: true
    delimiter: "^|^"
  
scenarios:
  - name: "Scenario testing non-successful API responses"
    flow:
      - log: "Posting incorrect login payload"
      - post:
          url: "/login-page-load"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            content-type: "application/json"
          json:
            username: "{{ username }}"
            password: "{{ password }}xyz"  # Wrong password
          expect:
            - statusCode: 401
            - hasProperty: 'error'
            - equals:
              - error: "Invalid credentials"
      - think: 2
      - log: "Sending request to invalid API"
      - get:
          url: "/login-page-load-invalid"  # Invalid URL
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 2
      - log: "Attempting to access protected resource without auth token"
      - get:
          url: "/login-page-load"
          expect:        # No headers added
            - statusCode: 403
            - hasProperty: 'error'
            - equals:
              - error: "No auth token provided"
