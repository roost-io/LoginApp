# ********RoostGPT********

# Test generated by RoostGPT for test Loginapp-api-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /login_post for http method type POST in artillery framework
# RoostTestHash=3bb15c01c5
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  environments:
    develop:
      target: "{{ $processEnvironment.API_HOST }}"
  payload:
    path: "login_post_others.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios: 
  - name: "Test Non-Success API Responses"
    flow:
    - log: "Attempt login with invalid username and password"
    - post:
        url: "/logn"
        headers:
          authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
        json:
          username: "{{ username }}"
          password: "{{ password }}"
        expect:
          - statusCode: 401
    - think: 1
    - log: "Valid credentials but invalid request method"
    - get:
        url: "/login"
        headers:
          authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 405
    - think: 1
    - log: "Testing mismatched content type"
    - post:
        url: "/login"
        headers:
          content-type: "text/html"
          authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
        json:
          username: "{{ username }}"
          password: "{{ password }}"
        expect:
          - statusCode: 415
          - contentType: "application/json"
    - think: 1
    - log: "Valid login request but missing authorization token"
    - post:
        url: "/login"
        headers:
          authorization: "Bearer "
        json:
          username: "{{ username }}"
          password: "{{ password }}"
        expect:
          - statusCode: 401
          - hasProperty: "error"
    - think: 1
    - log: "Valid login request but invalid authorization token"
    - post:
        url: "/login"
        headers:
          authorization: "Bearer invalidToken"
        json:
          username: "{{ username }}"
          password: "{{ password }}"
        expect:
          - statusCode: 403
          - hasProperty: "error"
    - think: 1
