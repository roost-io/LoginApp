# ********RoostGPT********

# Test generated by RoostGPT for test Loginapp-api-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /password_post for http method type POST in artillery framework
# RoostTestHash=7fbcf34287
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "password_post_success.csv"
    fields:
      - "text"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Send text to the password field"
    flow:
      - log: "POST Request - Send text to password field"
      - post:
          url: "/password"
          json:
            text: "{{ text }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "text"
      - think: 1
