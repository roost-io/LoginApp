# ********RoostGPT********

# Test generated by RoostGPT for test Loginapp-api-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /password_post for http method type POST in artillery framework
# RoostTestHash=7fbcf34287
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  headers:
    ContentType: "application/json"
    Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
  payload:
    path: "./password_post_others.csv"
    fields: ["text"]
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non Success API Response Test Cases"
    flow:
      - log: "Sending incorrect payload for /password endpoint"
      - post:
          url: "/password"
          json:
            text: ""
          expect:
            - statusCode: 400
      - think: 1

      - log: "Sending request to incorrect endpoint"
      - post:
          url: "/wrongEndpoint"
          json:
            text: "password123"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Sending request without authorization"
      - post:
          url: "/password"
          json:
            text: "password123"
          headers:
            Authorization: null
          expect:
            - statusCode: 401
      - think: 1
